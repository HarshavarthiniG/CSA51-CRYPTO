#include <stdio.h>
#include <string.h>
int main() {
    char c[100];
    int i, k, n;
    printf("Enter Ciphertext: ");
    scanf("%s", c);
    printf("Top 10 Possible Plaintexts:\n");
    for(k = 0; k < 10; k++) {
        printf("Key %d: ", k);
        for(i = 0; c[i] != '\0'; i++)
            printf("%c", ((c[i]-'A'-k+26)%26)+'A');
        printf("\n");
    }
    return 0;
}
