#include <stdio.h>
int main() 
{
    char text[100];
    int i;
    printf("Enter Plain Text: ");
    scanf("%s", text);
    printf("Encrypted Text: ");
    for(i = 0; text[i] != '\0'; i++) {
        printf("%c", text[i] + 1);
    }
    return 0;
}
